Norme:	loop_exec_pipe
	exec_with_env
	disp_env
	exec_cmd
	push_read
	rm_in_list

Gerer:	env -u USER > test

Check:
	check syntax > < >> <<
	do_redirections -> return de do_redi*
	norme loop_exec_pipe
	      push_read
	      rm_in_list